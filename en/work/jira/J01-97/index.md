---
layout: page
title: "J01-97: Complete the feature matrix for tooling operations"
permalink: /en/work/jira/J01-97/
---

**Stand:** 2026-03-16

Canonical public work status for `J01-97`.
This step extends the new Jira/docs tooling feature matrix with missing
operations, especially from `atlassian-tools` and CLI/build-adjacent areas.

## Goal

Complete the canonical matrix under
`areas/proj-doku-tooling/feature-matrix/` so that it does not only cover the
initial Jira/pages sync helpers.

## Current starting point

- `J01-92` introduced the `feature-matrix` doc type and created the first
  Jira/docs tooling instance.
- The current matrix mainly covers the recently moved Jira/pages helpers.
- More operations are still missing, for example:
  - tooling operations from `atlassian-tools`
  - CLI/build-adjacent operations, where they belong in this matrix or a
    derived follow-up matrix

## Backlog focus

- keep it in the backlog, not in the active sprint
- collect missing tooling operations and classify them on sub-feature level
- decide which CLI/build operations belong into this matrix and which should
  become their own follow-up matrix
- tighten parent features, sub-features, and states in the tooling sequence

## Links

- [Feature matrix: Jira and docs tooling]({{ "/en/areas/proj-doku-tooling/feature-matrix/" | relative_url }})
- [Jira work docs]({{ "/en/work/jira/" | relative_url }})
